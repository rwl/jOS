package jos.api.uikit;

import com.google.j2objc.annotations.Bind;

public enum UITextStorageDirection {
    @Bind("UITextStorageDirectionForward") FORWARD,
    @Bind("UITextStorageDirectionBackward") BACKWARD;
}
